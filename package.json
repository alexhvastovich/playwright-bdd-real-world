{
  "name": "playwright-bdd-example",
  "version": "0.1.0",
  "scripts": {
    "clean": "rimraf .features-gen",
    "bddgen": "npx bddgen",
    "playwright-test": "npx playwright test --reporter=html,list",
    "test": "npm run clean && npm run bddgen && npm run playwright-test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@cucumber/cucumber": "^10.6.0",
    "@playwright/test": "^1.43.1",
    "@types/node": "^20.12.7",
    "dotenv": "^16.4.5",
    "playwright-bdd": "^6.3.0",
    "rimraf": "^5.0.5"
  }
}
